class ApiEndPoints {
  
  static const products = "products";// Constante que define el endpoint para obtener los productos  
  static const popularMovies = "movie/popular";// Constante que define el endpoint para obtener las películas populares  
  static const upcomingMovies = "movie/upcoming";// Constante que define el endpoint para obtener las películas próximas  
  static const getGenreList = "genre/movie/list";// Constante que define el endpoint para obtener la lista de géneros de películas
}